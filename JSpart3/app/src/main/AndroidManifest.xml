<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saad.jspart3">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera2" />

    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo1"
        android:label="Job Seekicious"
        android:name="android.support.multidex.MultiDexApplication"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:theme="@style/splashstyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp1" />
        <activity android:name=".PostJob1" android:label="Post A Job" />
        <activity android:name=".SearchJob1" android:label="Search Job"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".wait" />
        <activity android:name=".JobList" android:label="Jobs"/>
        <activity android:name=".the_Job" android:label="Job Description" />
        <activity
            android:name=".activity_welcome"
            android:label="Sign Up" />
        <activity
            android:name=".user_profile_activity"
            android:label="Account Info"
            android:theme="@style/AppTheme" />
        <activity android:name=".employees_list" />
        <activity android:name=".employer_job" />
        <activity android:name=".employer_employee_relation" />
        <activity android:name=".DP_Activity" />
        <activity android:name=".userMessages" />
        <activity android:name=".userCareer" />
        <activity android:name=".ChatActivity" />
        <activity
            android:name="._chatActivity"
            android:label="Messenger" />
        <activity
            android:name="._signinActivity"
            android:label="Sign In" />
        <activity
            android:name=".JobSeekicious"
            android:label="@string/title_activity_job_seekicious"
            android:theme="@style/AppTheme" />
        <activity android:name="._GoogleActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="._mapActivity"
            android:label="@string/title_activity__map"></activity>
    </application>

</manifest>